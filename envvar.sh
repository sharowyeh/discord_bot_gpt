#!/bin/bash

# fill up token and api key from discord and openai developer site
export DISCORD_BOT_TOKEN=
# default guild id can leave empty if not specify 
#export DEFAULT_GUILD_ID=
export OPENAI_API_KEY=
